
set(CLIENT_LIBS ${PROTOBUF_LIBRARY})
ADD_LIBRARY(latte SHARED client.cc $<TARGET_OBJECTS:common> $<TARGET_OBJECTS:proto>)
TARGET_LINK_LIBRARIES(latte ${CLIENT_LIBS})
install(TARGETS latte
  LIBRARY DESTINATION lib)
install(FILES ${PROJECT_SOURCE_DIR}/client/latte
  DESTINATION bin)
